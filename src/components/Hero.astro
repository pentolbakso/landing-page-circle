---
import { Button } from "./ui/button";
---

<section
  class="min-h-screen flex flex-col items-center justify-center px-4 sm:px-6 lg:px-8"
>
  <div
    class="max-w-2xl mx-auto text-center space-y-6 bg-white/80 backdrop-blur-sm rounded-3xl p-6 sm:bg-transparent sm:backdrop-blur-none sm:p-0"
  >
    <!-- Main Heading -->
    <h1
      class="text-4xl sm:text-5xl md:text-6xl font-bold tracking-tight text-gradient animate-fadeIn"
      style="animation-delay: 0.1s; opacity: 0;"
    >
      One hub. Your tools.
    </h1>

    <!-- Subheading -->
    <p
      class="text-base sm:text-lg md:text-xl text-gray-400 max-w-xl mx-auto leading-relaxed animate-slideUp"
      style="animation-delay: 0.3s; opacity: 0;"
    >
      Connect all your favorite apps and services into one unified workspace.
      Your tools orbit around you — always in sync, always within reach. No more
      switching tabs, just seamless flow.
    </p>

    <!-- CTA Buttons -->
    <div
      class="flex flex-col sm:flex-row items-center justify-center gap-4 pt-2 animate-slideUp"
      style="animation-delay: 0.5s; opacity: 0;"
    >
      <Button
        variant="ghost"
        size="default"
        className="text-sm sm:text-base px-6 py-4"
        client:load
      >
        Watch demo
      </Button>

      <Button
        variant="outline"
        size="default"
        className="text-sm sm:text-base px-6 py-4"
        client:load
      >
        Start free
      </Button>

      <Button
        variant="ghost"
        size="default"
        className="text-sm sm:text-base px-6 py-4"
        client:load
      >
        View integrations
      </Button>
    </div>
  </div>
</section>
